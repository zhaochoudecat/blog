---
title: "博客操作手册"
date: 2026-05-03
---

# Hexo 博客简易操作手册

这份手册将帮助你快速掌握博客的日常使用。

## 1. 日常写作与发布

这是你最常用的功能。

### 第一步：创建新文章
在终端（博客根目录下）输入：
```bash
hexo new "我的新文章标题"
```
*   这会在 `source/_posts/` 目录下生成一个名为 `我的新文章标题.md` 的文件。
*   你可以使用任何 Markdown 编辑器（如 Typora, VS Code, Obsidian）打开并编辑它。

### 第二步：本地预览（推荐）
写完后，想看看效果？
```bash
hexo s
```
*   然后在浏览器打开 [http://localhost:4000](http://localhost:4000) 查看。
*   按 `Ctrl + C` 停止预览。

### 第三步：发布到 GitHub
确认无误后，执行“发布三部曲”：
```bash
hexo clean   # 清除旧的缓存（可选，建议执行以避免奇怪问题）
hexo g       # Generate：生成静态网页
hexo d       # Deploy：发布到 GitHub
```
*   稍等几分钟，访问 `https://zhaochoudecat.github.io` 即可看到更新。

---

## 2. 配置博客基本信息

想修改博客标题、作者或语言？

1.  打开博客根目录下的 **`_config.yml`** 文件。
2.  找到以下部分进行修改：

```yaml
# Site
title: 我的技术博客          # 博客标题
subtitle: 记录学习与生活      # 副标题
description: 这是一个关于...  # 网站描述（用于SEO）
keywords:
author: zhaochoudecat     # 你的名字
language: zh-CN           # 语言（建议改为 zh-CN 显示中文）
timezone: Asia/Shanghai   # 时区（可选）
```
3.  保存后，执行 `hexo g` 和 `hexo d` 更新。

---

## 3. 更换主题

Hexo 默认主题比较简单，换个好看的主题能让博客焕然一新。以流行的 **Next** 主题为例：

### 第一步：下载主题
在博客根目录下运行：
```bash
npm install hexo-theme-next --save
```
*(注：不同主题安装方式略有不同，有的需要 `git clone`，请参考具体主题文档)*

### 第二步：启用主题
1.  打开 **`_config.yml`** 文件。
2.  找到 `theme:` 这一行。
3.  修改为：
    ```yaml
    theme: next
    ```

### 第三步：验证
执行 `hexo clean` 和 `hexo s`，在本地查看新主题效果。

---

## 4. 常用命令速查表

| 命令 | 简写 | 作用 |
| :--- | :--- | :--- |
| `hexo new "..."` | `hexo n` | 新建文章 |
| `hexo generate` | `hexo g` | 生成静态页面 |
| `hexo server` | `hexo s` | 启动本地预览服务器 |
| `hexo deploy` | `hexo d` | 发布到远程仓库 |
| `hexo clean` | 无 | 清除缓存（解决样式不更新等问题） |

## 5. 常见问题
- **发布后没变化？** GitHub Pages 有缓存，通常需要等待 1-3 分钟。
- **文章里的图片怎么弄？** 建议使用图床（如 GitHub 图床、SM.MS），或者安装 `hexo-asset-image` 插件来管理本地图片。
