# 博客项目初始化指南

## 项目结构

```
blog/
├── .gitignore                # 排除 themes/、source/_posts、source/assets 等
├── _config.yml               # Hexo 主配置
├── _config.next.yml          # Next 主题配置 (wwdc16 配色、复制按钮)
├── package.json              # 依赖声明
├── scaffolds/                # 文章模板
├── source/
│   ├── categories/           # 分类页
│   ├── tags/                 # 标签页
│   ├── images/               # 图片资源
│   ├── _posts → (symlink)   # 软链接，指向 Obsidian CTF 笔记目录
│   └── assets  → (symlink)  # 软链接，指向 Obsidian assets 目录
└── themes/
    └── next/                 # 独立 git 仓库，fork 自 hexo-theme-next
```

## 仓库关系

| 仓库 | 地址 | 内容 |
|------|------|------|
| 博客配置 | `zhaochoudecat/blog` | 配置文件、scaffolds、页面 |
| 主题 | `zhaochoudecat/hexo-theme-next` | wwdc16 配色、复制按钮等 |

两个仓库独立维护，互不影响。

## 换电脑恢复步骤

```bash
# 1. 克隆博客仓库
git clone git@github.com:zhaochoudecat/blog.git
cd blog

# 2. 克隆主题仓库
git clone git@github.com:zhaochoudecat/hexo-theme-next.git themes/next

# 3. 安装依赖
npm install

# 4. 创建软链接（根据实际路径调整）
ln -s /path/to/obsidian source/_posts
ln -s /path/to/obsidian/assets source/assets

# 5. 预览
hexo server
```

## 日常更新

```bash
# 更新博客配置
git add -A && git commit -m "描述改动" && git push

# 更新主题
cd themes/next
git add -A && git commit -m "描述改动" && git push
```
